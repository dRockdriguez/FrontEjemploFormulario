import { PeticionesPipe } from './peticiones.pipe';

describe('PeticionesPipe', () => {
  it('create an instance', () => {
    const pipe = new PeticionesPipe();
    expect(pipe).toBeTruthy();
  });
});
